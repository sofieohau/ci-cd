name: CI/CD Pipeline

on:
  pull_requests:
    types:
      - opened
      - synchronized

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: echo "Yolo"

      - name: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
          echo "This code runs only on pull requests"
          fi
